import React from 'react'
import DefaultTemplate from 'src/templates/DefaultTemplate'

export default class NewBlocksPage extends React.Component {
  render () {
    return (
      <DefaultTemplate>
        TODO: Show latest block(s) in the chain
      </DefaultTemplate>
    )
  }
}
